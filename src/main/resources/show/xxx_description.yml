#xxx_description.yml仅仅作为描述文件方便查看,不会被程序读取使用
#==================================
#注意替换符不要与下面这些内置替换符冲突:
#=============内置替换符=============
#{name}:  玩家名
#{displayName}:  玩家显示名
#{pageNow}:  当前页
#{pageMax}:  最大页
#{listSize}:  列表分页大小
#{listNow}:  列表当前页
#{listMax}:  列表最大页
#{getSize}:  获取到的列表大小
#=============额外替换符=============